<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.devmaker.finances">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_financas"
        android:label="+Control"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RelatorioActivity"></activity>
        <activity android:name=".DescricaoActivity"></activity>
        <activity android:name=".PlanejamentoActivity"></activity>
    </application>

</manifest>